# Copie este arquivo para .env (sem o .example) e preencha com seus valores reais.
# Este arquivo NUNCA deve ser commitado — já está no .gitignore.

API_FOOTBALL_KEY=

# Deixe as duas linhas abaixo vazias/comentadas para os scripts usarem o SQLite local.
# Preencha só quando quiser rodar contra o banco de produção (Turso).
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
